const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  content: ['theme/**/*.liquid'],
  theme: {
    extend: {
      
    }
  },
  variants: {},
  plugins: [],
  corePlugins: {
    container: false
  }
}